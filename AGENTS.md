# Agents: task-management

## Scope

- MCP server and HTTP API; follow `README.md` and `CURSOR_INTEGRATION.md` for Cursor wiring.

## Logging

- If deployed alongside Statex stack: send structured logs to **logging-microservice** when configured.

## Commands

- Local: see `package.json` scripts and `setup.sh` / `setup-docker.sh`.
